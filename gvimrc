set visualbell

colorscheme desert256

if has("gui_running")
  set guioptions=egmrt
endif

set lines=35
set columns=125
"set guicursor=a:hor10-blinkon1

if has("macunix")
  set guifont=Monaco:h14
else
  set guifont=Monaco\ 14
endif

